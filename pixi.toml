[workspace]
name = "exporter"
version = "0.1.0"
description = "A development environment for a C++ library with Python bindings, integrated with ONNX Runtime and PyTorch."
authors = ["TODO"]
channels = ["conda-forge"]
platforms = ["linux-64"]

[system-requirements]
libc = { family = "glibc", version = "2.35" }

[activation.env]
LD_LIBRARY_PATH = "$CONDA_PREFIX/lib:$LD_LIBRARY_PATH"

[dependencies]
python = "3.11.*"
git = "*"
pip = "*"
onnx = "*"
onnxscript = "*"
protobuf = "*"
onnxruntime-cpp = ">=1.15"
pybind11 = ">=2.10"
cmake = "=3.24"
ninja = "*"
eigen = ">=3.4"
nlohmann_json = ">=3.11"
cpp-expected = "*"
fmt = ">=9.1"
gtest = "*"
gmock = "*"
pytest = "*"
gymnasium = "==1.2.0"
tqdm = "*"
tensorboard = "*"
typing_extensions = "==4.12.2"
numpy = "==1.26.0"
pillow = "==11.3.0"
gcc_linux-64 = "*"
gxx_linux-64 = "*"
packaging = "==23.0"
markupsafe = "==2.1.3"
libstdcxx-ng = "*"
psutil = "==5.9.8"
click = "==8.1.7"

# Linting and formatting tools
ruff = "*"
clang-format = "*"

[pypi-options]
extra-index-urls = ["https://pypi.nvidia.com"]

[pypi-dependencies]
torch = { version = "==2.7.0", index = "https://download.pytorch.org/whl/cu128" }
torchvision = { version = "==0.22.0", index = "https://download.pytorch.org/whl/cu128" }
isaacsim = { version = "==5.1.0", extras = ["all", "extscache"] }
onnxruntime = "*"
pytest-gitignore = "*"

[tasks]
# General tasks
clone-isaaclab = "git clone https://github.com/isaac-sim/IsaacLab.git"
test-import = "python -c \"import isaaclab; print('Isaac Lab installed:', isaaclab.__file__)\""
test-torch = "python -c \"import torch; print('CUDA available:', torch.cuda.is_available())\""
verify = { depends-on = ["test-import", "test-torch"] }
pixi-install = { cmd = "pixi install" }
install-isaaclab = { cmd = "cd IsaacLab && chmod +x isaaclab.sh && ./isaaclab.sh --install", depends-on = ["clone-isaaclab"] }
install-exporter = "pip install -e ."
setup = { depends-on = ["pixi-install", "install-isaaclab", "install-exporter"] }
configure = "cmake -S . -B build -G Ninja"
build = "cmake --build build"
test-cpp = "ctest --test-dir build"
test-python = "pytest -s ."
test-all = { depends-on = ["test-python", "test-cpp"] }
test = { depends-on = ["test-all"] }
install-python = "pip install -e python/"
clean = "rm -rf IsaacLab .pixi pixi.lock"

# Examples
export-isaaclab = "python examples/exporter_scripts/export_isaaclab.py"

## Isaac Sim
isaac-sim = { cmd = "isaacsim --disable-extensions isaacsim.ros2.bridge" }

## Isaac Lab demos
lab-arms = "python IsaacLab/scripts/demos/arms.py"
lab-quadrupeds = "python IsaacLab/scripts/demos/quadrupeds.py"
lab-cartpole = "python IsaacLab/scripts/demos/cartpole.py"
lab-markers = "python IsaacLab/scripts/demos/markers.py"

# Linting and formatting
# Python
lint-python = "ruff check ."
lint-python-fix = "ruff check --fix ."
format-python = "ruff format ."
format-python-check = "ruff format --check ."
# C++
format-cpp = "clang-format -i **/*.cpp **/*.hpp"
format-cpp-check = "clang-format --dry-run **/*.cpp **/*.hpp"
# Combined
format = { depends-on = ["format-python", "format-cpp"] }
format-check = { depends-on = ["format-python-check", "format-cpp-check"] }
lint = { depends-on = ["lint-python"] }
check = { depends-on = ["lint", "format-check"] }

